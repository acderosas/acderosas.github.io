export default function AboutMe() {
    return (
        <main>
            <h2>About Me</h2>
        </main>
    )
}

// To create nested routes add another folder inside current folder with its own page.jsx